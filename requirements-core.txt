# Core dependencies – no C++/CMake build required (use this if full requirements.txt fails on Windows)
# Omit: face-recognition (needs dlib + CMake)
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
pydantic==2.5.3
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
python-dotenv==1.0.0
APScheduler==3.10.4

# Optional: image/numpy (have wheels; needed if you re-enable face features later)
opencv-python==4.8.1.78
Pillow==10.1.0
numpy>=1.26.0,<2

# Background validation (YOLOv8 + room reference similarity; pulls torch on install)
ultralytics>=8.0.0,<9
