# Optional: campus URL shown in `npm run dev` (default is 10.4.214.75 if unset).
# Set your real IPv4 on that network from `ipconfig` if different.
# VITE_NETWORK_HINT_HOST=10.4.214.99
# To hide the extra line: VITE_NETWORK_HINT_HOST=0
